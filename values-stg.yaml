
# Docker socket for Testcontainers
volumes:
  - name: docker-socket
    hostPath:
      path: /var/run/docker.sock
      type: Socket

volumeMounts:
  - name: docker-socket
    mountPath: /var/run/docker.sock
